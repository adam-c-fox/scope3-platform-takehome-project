# Required: Your Scope3 API key for accessing the /measure endpoint
SCOPE3_API_KEY=your_api_key_here

# Optional: Log level (debug, info, warn, error, fatal). Defaults to 'info'
LOG_LEVEL=info

# Optional: Server port. Defaults to 3000
PORT=3000
